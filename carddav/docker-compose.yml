services:
  radicale:
    build: ./radicale
    volumes:
      - carddav_data:/data
    restart: unless-stopped
    networks:
      - coolify
      - default
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.radicale.rule=Host(`carddav.pipeleads.app`)"
      - "traefik.http.routers.radicale.entrypoints=https"
      - "traefik.http.routers.radicale.tls=true"
      - "traefik.http.routers.radicale.tls.certresolver=letsencrypt"
      - "traefik.http.services.radicale.loadbalancer.server.port=5232"
      - "traefik.docker.network=coolify"

  sync-service:
    build: ./sync-service
    volumes:
      - carddav_data:/data
    environment:
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      - INTERNAL_SECRET=${INTERNAL_SECRET}
      - CARDDAV_DATA_PATH=/data
      - RADICALE_URL=http://radicale:5232
      - CARDDAV_BASE_URL=https://carddav.pipeleads.app
      - CARDDAV_PASSWORD_SECRET=${CARDDAV_PASSWORD_SECRET}
      - SUPABASE_REALTIME_URL=http://10.0.5.11:8000
    depends_on:
      - radicale
    restart: unless-stopped
    networks:
      - coolify
      - default
      - supabase

networks:
  coolify:
    external: true
  supabase:
    external: true
    name: fqg7al0xzfytzza2hgwth6ab
  default:

volumes:
  carddav_data:
